<?xml version="1.0"?>
<!DOCTYPE project>

<project name="SPModels" basedir="." default="deploy" xmlns:antelope="antlib:ise.antelope.tasks">
	<property name="project.dir" value=".." />

	<import file="../build-common.xml" />

	<target name="clean">
		<delete dir="classes"/>
		<delete dir="jar"/>
		<delete file="${app.server.lib.global.dir}/SPModels.jar" />
	</target>

	<target name="compile" depends="clean">
		<mkdir dir="classes"/>
		<antcall target="compile-java">
			<param name="javac.classpathref" value="portal.classpath" />
			<param name="javac.destdir" value="classes" />
			<param name="javac.srcdir" value="src" />
		</antcall>
	</target>

	<target name="compile-java">
		<copy todir="${javac.destdir}">
			<fileset dir="${javac.srcdir}" excludes="**/*.java" />
		</copy>

		<javac
			classpathref="${javac.classpathref}"
			compiler="${javac.compiler}"
			debug="${javac.debug}"
			deprecation="${javac.deprecation}"
			destdir="${javac.destdir}"
			encoding="${javac.encoding}"
			includeAntRuntime="false"
			nowarn="${javac.nowarn}"
			srcdir="${javac.srcdir}"
		/>
	</target>

	<target name="jar" depends="compile">
		<mkdir dir="jar"/>
		<jar
			destfile="jar/SPModels.jar"
			basedir="classes"
			includes="**"
			excludes="*.jar"
		/>
	</target>

	<target name="deploy" depends="jar">
		<if>
			<available file="jar/SPModels.jar" />
			<then>
				<copy file="jar/SPModels.jar" todir="${app.server.lib.global.dir}" overwrite="true"/>
			</then>
			<else>
				<echo message="SPModels.jar doesn't exist" />
			</else>

		</if>
	</target>
</project>